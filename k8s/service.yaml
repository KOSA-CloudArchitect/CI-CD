# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: web-server-service
spec:
  selector:
    app: web-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  # 외부에서 접속할 수 있도록 LoadBalancer 타입으로 설정합니다.
  type: LoadBalancer
